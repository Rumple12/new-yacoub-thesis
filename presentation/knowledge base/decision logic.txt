decision logic




temperature event
	* image - SHOW code TERMINAL HIGHBODY/LOWBODY EVENT TRIGGER
Why temp sensor used
* just a simple example to display the archtecture implemenation on a constrained task 
	* no GPIO pins connect to raspberry pi for real sensors
	* used mock tempterature event thru terminal
	* MENTION HARDWARE SERVER HERE 
* that still proves the archietcture
-
* DEFENSE - (Later, GPIO/real sensor can be added as optional Raspberry Pi validation, not thecore requirement.)'
	* show hypothnetically how it would work with hardware integration
-
* (temperature high → fan_on) ... (temperature normal/low → fan_off)
* In the deterministic workflow, this is basically an IF node in n8n.
	* (maybe) show the n8n canvas on determinstic mode 
* In the agent-enhanced workflow, the decision logic is the AI/LLM node/prompt that returns 
	* (MAYBE) expand on how this agent enhanced works and how cool its application is 
		* show the n8n canvas on agent enhanced choice
a structured 	action like:  { "action_id": "fan_on", "target": "fan_1" }
