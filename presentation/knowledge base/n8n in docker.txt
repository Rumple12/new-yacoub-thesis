n8n in docker


(image)
-
docker logo, arrow to n8n logo
transparent
bulletins being listed



* docker setup 
	* Docker is used as the runtime layer for n8n, so the workflow engine runs in a 	controlled local container instead of being installed directly on the PC.
		* I used Docker to run n8n locally,
		* so I could start the whole workflow environment with one command 			instead of installing everything manually on the computer.
		* That made it easy to start the system
		*  So instead of installing n8n directly on my computer, I could start it 		with Docker Compose and open it at localhost port 5678.
-
* one deterministic workflow as the baseline
* one minimal agent-enhanced workflow as the AI comparison
	* gemeni nano model 
* (docker compose up) starts the n8n container:
	* http://localhost:5678
	* The docker-compose.yml file uses the official n8n image n8nio/n8n:1.123.37 and exposes 		n8n on 	port 5678.

